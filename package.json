{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "CORS proxy",
  "type": "module",
  "scripts": {
    "deploy": "serverless deploy",
    "offline": "serverless offline"
  },
  "dependencies": {
    "node-fetch": "^3.2.10"
  },
  "devDependencies": {
    "serverless-offline": "^10.0.2"
  }
}
